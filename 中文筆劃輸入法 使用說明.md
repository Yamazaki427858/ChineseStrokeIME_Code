# 中文筆劃輸入法 — 使用說明

本文說明 **ChineseStrokeIME（中文筆劃輸入法）** 的安裝、操作與設定，適用於 Windows 平台。輸入法以「筆劃編碼」輸入中文，不依賴系統 IME，透過鍵盤鉤子攔截按鍵並將選定文字送入目前焦點視窗。

**版本**：3.3.0（以 `ime_core.h` 之 `APP_VERSION` 為準）

---

## 一、簡介與安裝

### 1.1 什麼是中文筆劃輸入法

- **輸入方式**：以五種筆劃對應鍵盤 `u`、`i`、`o`、`j`、`k`（或小鍵盤 7/8/9/4/5），依字碼表（`system\Zi-Ma-Biao.txt`）查詢候選字。
- **運作方式**：獨立程式 + 低階鍵盤鉤子，不取代 Windows 內建 IME；選字後以 Unicode 送入目前焦點視窗。
- **免安裝**：將程式放在任意英文路徑即可執行，設定與學習紀錄保存在 `user\`、`system\` 子目錄。

### 1.2 目錄結構

程式以執行檔（`ChineseStrokeIME.exe`）所在目錄為根目錄：

| 目錄 | 用途 |
|------|------|
| **system\\** | 字碼表 `Zi-Ma-Biao.txt`、詞語庫 `wordphrases.txt` 與快取 `wordphrases.cache`、`positions.ini`、`version_cache.txt` 等 |
| **user\\** | 使用者字典 `user_dict.txt`、聯想學習 `context_learning.txt`、介面設定 `interfaceconfig.ini`、標點選單 `punctmenu.txt`、Emoji 資料 `emoji\` 等 |

**請將程式放在英文路徑下**（例如 `C:\ChineseStrokeIME\`），避免目錄名稱含中文或特殊字元造成讀寫異常。

若缺少字碼表，程式會嘗試從 GitHub 下載；若下載失敗，請手動將 `Zi-Ma-Biao.txt` 放入 `system` 目錄。

### 1.3 編譯與執行

- **編譯**（Windows + MinGW）：在專案根目錄執行 `mingw32-make`，產出 `ChineseStrokeIME.exe`。
- **清理**：`mingw32-make clean`。
- **執行**：直接執行 `ChineseStrokeIME.exe`。若無法安裝鍵盤鉤子，請嘗試以系統管理員權限執行。

---

## 二、啟動與介面

### 2.1 啟動後畫面

- 主視窗為 **OptimizedUI 工具列**（一條橫條），可拖曳移動。
- 系統托盤會出現「中文筆劃輸入法」圖示；右鍵可開啟快捷選單。
- 字碼輸入視窗、候選視窗、聯想視窗在「有輸入或候選」時才會出現，並跟隨工具列或滑鼠位置。
- 啟動約 3 秒後會自動檢查版本更新（可在設定檔關閉彈窗提醒，見第十節）。

### 2.2 主視窗（工具列）

**完整工具列**（由左至右大致為）：

- **模式指示**：點擊切換「中文 / 英文」。
- **狀態指示**：就緒、輸入中、錯誤、暫放、暫停等。
- **選單**：標點／Emoji、重新載入字碼表、GitHub 更新字碼表／Emoji、重新載入配置、半透明、聯想字、筆劃符號顯示、自訂筆劃五鍵、暫放、重置定位、縮小工具列、最小化、關於等。
- **暫放**：開啟／關閉暫放模式。
- **還原**：重置為「滑鼠跟隨」定位。
- **最小化**：縮到托盤。
- **關閉**：結束程式（會確認）。

**縮小工具列**（`toolbarClassicModeBadges=1` 或選單「縮小顯示工作列」）：

- 僅顯示 **「劃」** 與 **「E」** 兩鍵，占用空間更小。
- 在精簡列上**按右鍵**仍可開啟完整選單。

### 2.3 視窗拖曳與多螢幕

- 可拖曳字碼、候選或聯想視窗，整塊輸入區一起移動，並限制在目前螢幕工作區內。
- 位置寫入 `system\positions.ini`；延伸模式與鏡像模式分別記憶工具列位置。
- 熱插拔或延伸↔同步切換時，若工具列落在已移除的螢幕上，會自動移回主螢幕安全位置。

---

## 三、筆劃鍵與字碼輸入

### 3.1 筆劃鍵對應（預設）

| 鍵盤按鍵 | 小鍵盤 | 筆劃 | 說明 |
|----------|--------|------|------|
| **U** | Num 7 | 一（橫） | |
| **I** | Num 8 | 丨（豎） | |
| **O** | Num 9 | 丿（撇） | |
| **J** | Num 4 | 丶（點） | |
| **K** | Num 5 | フ（折） | |

- 可在 `user\interfaceconfig.ini` 的 `[InputSettings]` 自訂字母鍵與小鍵盤鍵（見第十節）；工具列選單「自訂筆劃五鍵」可快速開關。
- 輸入框可顯示「一丨丿丶フ」或「uiojk」，由 `showStrokeSymbols` 控制（1=筆劃符號，0=字母）。

### 3.2 字碼表格式

- 路徑：`system\Zi-Ma-Biao.txt`，UTF-8。
- 每行：**單字 + Tab + 筆劃碼**（僅含 u、i、o、j、k）。
- 範例：`的	oikuuokj`、`一	u`

### 3.3 輸入流程

1. 確認為 **中文模式**。
2. 依序按筆劃鍵，字碼視窗顯示目前字碼（或筆劃符號）。
3. 程式查表顯示 **候選字視窗**（一頁最多 9 個）。
4. 用 **數字鍵 1～9** 選字。
5. 選定的字送入目前焦點視窗的應用程式。

---

## 四、選字與翻頁

### 4.1 選字按鍵

| 操作 | 按鍵 |
|------|------|
| 選字 | **數字鍵 1～9**（對應候選第 1～9 個） |
| 上一頁 | **↑** 或候選視窗「上一頁」 |
| 下一頁 | **↓** 或候選視窗「下一頁」 |

本輸入法**僅以數字鍵 1～9 選字**，一般模式下 Enter、Space 會傳給焦點程式。僅在**暫放模式**下，焦點在暫放視窗、無候選且有內容時，Enter 才用來送出暫放文字。

### 4.2 輸入中功能鍵

| 按鍵 | 功能 |
|------|------|
| **Backspace** | 刪除最後一碼；聯想模式下可「反學習」剛選的聯想（見第六節） |
| **ESC** | 取消輸入，清空字碼與候選，並中斷聯想前文 |

### 4.3 候選字視窗右鍵（字碼候選）

在 **字碼候選模式**（非標點／Emoji 選單）下，於候選列上按右鍵：

- 顯示「用戶字典：〇〇」
- **刪除此候選字**：從 `user\user_dict.txt` 移除該字（僅對已加入用戶字典的字可用；主字碼表內的字仍可能出現，但不再有個人字典優先）

### 4.4 聯想字視窗右鍵（聯想管理）

在 **聯想字模式**下，於聯想列上按右鍵，可管理「前文 → 候選」關聯：

- **⚐ 設為置頂 (pinned)** / **取消置頂**
- **⚑ 設為鎖定 (locked)** / **取消鎖定**
- **刪除此聯想（可再次學回）**
- **永不再顯示此聯想** / **取消永不再顯示**

選單標題會顯示目前設定的「前字 → 後字」，避免刪錯關聯。

---

## 五、中英文、標點與 Emoji

### 5.1 中英文切換

- **Shift 單擊**（短按且未搭配其他鍵）：切換中文全形／英文半形。
- 工具列 **模式指示** 或托盤選單亦可切換。

### 5.2 英文輸入

- **中文模式**下：僅筆劃鍵、**P**、萬用字元鍵等由輸入法處理；其餘 **A～Z** 轉成英文字母送入目標視窗。
- **Shift + 字母**：先清空當前輸入，再輸出該英文字母。

### 5.3 標點符號與 Emoji（P 鍵）

- **P 鍵**（中文模式）：開啟 **標點／Emoji 選單**。
  - 預設為全形標點（內容可自訂 `user\punctmenu.txt`），以 1～9 或滑鼠選取。
  - 可切換至 **Emoji 分頁**，依分類瀏覽表情符號；選取後插入目前輸入位置，**ESC** 關閉選單。
- Emoji 資料在 `user\emoji\`；首次缺檔可自動從 GitHub 下載，亦可從選單「從 GitHub 更新 Emoji」手動更新。
- 鍵盤 **, . / ;** 等仍依標點表輸出全形／半形（中文／英文模式不同）。

---

## 六、聯想字（智能聯想）

### 6.1 何時出現聯想

- 選完字後，依前文推薦下一個字，顯示在 **聯想字視窗**。
- 聯想來源包括：
  - 字碼表內建相鄰字對（bigram）
  - 大型詞語庫 `system\wordphrases.txt`（多字前綴接續，例如「香港」→「貿易」）
  - 個人學習 `user\context_learning.txt`

### 6.2 詞語庫下載

- 本地檔：`system\wordphrases.txt`（每行一個詞語，UTF-8）。
- 官方下載：https://github.com/Yamazaki427858/ChineseStrokeIME/tree/ChineseStrokeIME/SourceCode/%E8%81%AF%E6%83%B3%E8%A9%9E%E5%BA%AB  
  請下載 **`word_phrases.txt`** 存為 **`system\wordphrases.txt`**。
- 若本地無檔且網路可用，啟動時會嘗試自動下載；更新 txt 後重啟即可，快取 `wordphrases.cache` 會自動重建。

### 6.3 聯想條目模式

| 模式 | 說明 |
|------|------|
| **locked（鎖定）** | 該「前字→後字」永遠排第一（⚑） |
| **pinned（置頂）** | 置頂且加分（⚐） |
| **自動學習** | 依使用次數排序 |
| **永不再顯示** | 右鍵封鎖，可再取消 |

### 6.4 開關與進階

- 工具列選單 **「聯想字功能」** 或 `enable_word_prediction=1` 開啟（預設生成設定檔可能為 0，需手動開啟）。
- `max_word_predictions`：聯想視窗最多顯示候選數（1～1000，預設 100）。
- 選錯聯想：在聯想視窗按 **Backspace** 可撤銷剛學的關聯（反學習）。
- **ESC、標點、切換中英文、Enter、退格（字碼已空）** 等會中斷聯想前文，避免跨句誤學。

---

## 七、3+3 模式與萬用字元

### 7.1 萬用字元 *

- 字碼中 **\*** 代表任意一碼，用於模糊查詢。
- 預設鍵：**L**、**NumPad0**；可在 `[InputSettings]` 設定 `wildcardKey1`、`wildcardKey2`。

### 7.2 3+3 建議

- 輸入碼較長時，狀態列會提示「前三碼*後三碼」等，縮小候選範圍。

---

## 八、暫放視窗與剪貼簿模式

### 8.1 暫放模式

- **用途**：先輸入或貼上文字到暫放區，再一次性送出或存檔；支援 Undo/Redo。
- **開啟**：工具列 **暫放** 或托盤「開啟暫放模式」。
- 暫放模式下仍可使用筆劃、**P**（標點／Emoji）、1～9 選字。

### 8.2 暫放區操作

- 方向鍵、Home/End 移動游標；Backspace/Delete 刪字。
- **Enter**（無候選時）：送出暫放內容到焦點視窗。
- 視窗按鈕可送出、清除或存檔。

### 8.3 剪貼簿模式

- `interfaceconfig.ini` → `clipboard_mode=1` 開啟。
- 可將暫放內容複製到剪貼簿，或從剪貼簿貼上後再送出。

---

## 九、托盤與快捷功能

### 9.1 托盤圖示

- **左鍵**：顯示或置前主視窗。
- **右鍵**：托盤選單。

### 9.2 托盤選單（主要項目）

| 項目 | 功能 |
|------|------|
| 顯示輸入法 / 置前 | 顯示主視窗 |
| 切換英文/中文模式 | 中英文切換 |
| 標點符號選單 | 等同按 P 開啟標點／Emoji |
| 發送暫放文字 / 清空暫放區 | 暫放模式下可用 |
| 開啟暫放模式 | 未開暫放時顯示 |
| 重置為滑鼠跟隨 / 取消固定位置 | 定位相關 |
| 重新載入配置 | 重讀 `interfaceconfig.ini` 等 |
| 從 GitHub 更新字碼表 | 覆蓋 `Zi-Ma-Biao.txt`（請先備份自訂） |
| 從 GitHub 更新 Emoji | 更新 `user\emoji\` |
| 關於 | 版本說明；可手動檢查更新 |
| 重啟 / 關閉輸入法 | 重啟或結束程式 |

---

## 十、設定檔（interfaceconfig.ini）

路徑：**user\interfaceconfig.ini**。修改後可用「重新載入配置」套用，部分項目重啟更保險。

### 10.1 區段摘要

| 區段 | 主要內容 |
|------|----------|
| **[Colors]** | 各視窗與按鈕顏色（#RRGGBB） |
| **[Font]** | 字型名稱與大小 |
| **[Window]** | 主視窗、候選、字碼視窗寬高 |
| **[WindowBehavior]** | 置頂、半透明、聯想字、候選字碼顯示、筆劃符號、螢幕提示、縮小工具列、版本更新不再提醒等 |
| **[InputSettings]** | 萬用字元鍵、自訂筆劃五鍵（字母／小鍵盤） |
| **[MultiScreenSettings]** | 多螢幕模式變更提示（與 WindowBehavior 相容） |

### 10.2 常用選項範例

```ini
[WindowBehavior]
enable_word_prediction=1       ; 1=聯想字開啟
max_word_predictions=100       ; 聯想候選數上限（1～1000）
showStrokeSymbols=1            ; 1=一丨丿丶フ，0=uiojk
showCandidateCode=0            ; 1=候選顯示字根如 十[ui]
toolbarClassicModeBadges=0     ; 1=縮小工具列（僅劃／E）
suppress_version_update_reminder=0  ; 1=啟動時不再彈出版本更新
force_stay_on_top=1
enable_transparency=0
transparency_alpha=220
clipboard_mode=0
showScreenModeNotification=0   ; 1=螢幕模式變更時彈窗

[InputSettings]
wildcardKey1=L
wildcardKey2=NumPad0
enableCustomStrokeKeys=0
strokeKeyU=U
strokeKeyI=I
strokeKeyO=O
strokeKeyJ=J
strokeKeyK=K
enableCustomNumpadStrokeKeys=0
numpadStrokeKeyU=NumPad7
numpadStrokeKeyI=NumPad8
numpadStrokeKeyO=NumPad9
numpadStrokeKeyJ=NumPad4
numpadStrokeKeyK=NumPad5
```

### 10.3 版本更新提醒

- 啟動後若偵測到新版本，可選 **前往下載**、**稍後** 或 **不再提醒**。
- 選「不再提醒」會寫入 `suppress_version_update_reminder=1`；改回 `0` 並重啟可恢復。
- 「關於」內的手動檢查更新不受此項影響。

---

## 十一、進階與疑難排解

### 11.1 暫停輸入法

- 選單 → **暫停輸入法**：釋放鍵盤鉤子，按鍵不再被攔截。
- 再選 **啟用輸入法** 恢復。

### 11.2 按鍵放行

- **Ctrl、Win、Alt** 組合鍵一律放行，系統快捷鍵可正常使用。

### 11.3 與系統 IME

- 本程式以鍵盤鉤子攔截筆劃鍵、選字鍵等；若仍有衝突，可將 Windows 預設輸入法切到英文鍵盤再使用。

### 11.4 字碼表與詞語庫更新

- **字碼表**：選單「從 GitHub 更新字碼表」；有自訂請先備份。
- **詞語庫**：見第六節 GitHub 連結；檔名存為 `system\wordphrases.txt`。

### 11.5 使用者字典與聯想學習

- **user_dict.txt**：使用過的字會影響排序；候選右鍵可刪除指定條目。
- **context_learning.txt**：聯想學習紀錄；**手動編輯前請關閉程式**，避免定時寫入覆蓋。

---

## 十二、快速參考

| 項目 | 說明 |
|------|------|
| 筆劃鍵 | U/I/O/J/K（小鍵盤 7/8/9/4/5） |
| 萬用字元 | L 或 NumPad0（可設）→ * |
| 標點／Emoji | P（中文模式） |
| 選字 | 數字鍵 1～9 |
| 翻頁 | ↑ / ↓ |
| 取消輸入 | ESC |
| 中英切換 | Shift 單擊 或 模式鈕 |
| 候選右鍵 | 刪除用戶字典候選字 |
| 聯想右鍵 | 置頂、鎖定、刪除、永不再顯示 |
| 聯想反學習 | 聯想窗 Backspace |

---

以上內容依目前程式碼整理。版本更新與新功能詳述請參考專案內 **v3.3.0更新說明.md**；若與實際介面有出入，以執行檔行為為準。
