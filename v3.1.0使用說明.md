# 中文筆劃輸入法 — 使用說明

本文說明 **ChineseStrokeIME（中文筆劃輸入法）** 的安裝、操作與設定，適用於 Windows 平台。輸入法以「筆劃編碼」輸入中文，不依賴系統 IME，透過鍵盤鉤子攔截按鍵並將選定文字送入目前焦點視窗。

---

## 一、簡介與安裝

### 1.1 什麼是中文筆劃輸入法

- **輸入方式**：以五種筆劃對應鍵盤 `u`、`i`、`o`、`j`、`k`，依字碼表（如 `system\Zi-Ma-Biao.txt`）查詢候選字。
- **運作方式**：獨立程式 + 低階鍵盤鉤子，不取代 Windows 內建 IME；選字後以 Unicode 送入目前焦點視窗。
- **版本**：目前為 3.1.0（以 `ime_core.h` 之 `APP_VERSION` 為準）。

### 1.2 目錄結構

程式以執行檔（`ChineseStrokeIME.exe`）所在目錄為根目錄，會自動使用並建立以下子目錄：

| 目錄 | 用途 |
|------|------|
| **system\\** | 字碼表 `Zi-Ma-Biao.txt`、詞語庫 `wordphrases.txt`、`positions.ini`、`version_cache.txt` 等 |
| **user\\** | 使用者字典 `user_dict.txt`、聯想學習 `context_learning.txt`、介面設定 `interfaceconfig.ini`、標點選單 `punctmenu.txt` 等 |

**請將程式放在英文路徑下**（例如 `C:\ChineseStrokeIME\`），避免目錄名稱含中文或特殊字元造成讀寫異常。

若缺少字碼表，程式會嘗試從 GitHub 下載；若下載失敗，請手動將 `Zi-Ma-Biao.txt` 放入 `system` 目錄。

### 1.3 編譯與執行

- **編譯**：在專案根目錄執行 `make`，產出 `ChineseStrokeIME.exe`。
- **清理**：`make clean`。
- **執行**：直接執行 `ChineseStrokeIME.exe`。若無法安裝鍵盤鉤子，請嘗試以系統管理員權限執行。

---

## 二、啟動與介面

### 2.1 啟動後畫面

- 主視窗為 **OptimizedUI 工具列**（一條橫條），可拖曳移動。
- 系統托盤會出現「中文筆劃輸入法」圖示；右鍵可開啟快捷選單。
- 字碼輸入視窗、候選視窗、聯想視窗在「有輸入或候選」時才會出現，並跟隨工具列或滑鼠位置。

### 2.2 主視窗（工具列）按鈕

從左到右大致為：

- **模式指示**：點擊可切換「中文 / 英文」模式。
- **狀態指示**：顯示就緒 / 輸入中 / 錯誤 / 暫放 / 暫停等狀態。
- **選單**：標點選單、重新載入字碼表、從 GitHub 更新字碼表、重新載入配置、半透明、聯想字、筆劃符號顯示、暫停/啟用輸入法、關於。
- **暫放**：開啟/關閉暫放模式（暫放區視窗）。
- **還原**：重置為「滑鼠跟隨」定位（取消固定位置）。
- **最小化**：縮到托盤。
- **關閉**：結束程式（會確認）。

### 2.3 視窗拖曳

- 可拖曳 **字碼輸入視窗**、**候選視窗** 或 **聯想視窗** 的標題/空白區域，整塊輸入區會一起移動，並限制在目前螢幕工作區內。
- 位置會記憶，下次啟動可還原（含延伸/鏡像等多螢幕模式）。

---

## 三、筆劃鍵與字碼輸入

### 3.1 筆劃鍵對應

| 鍵盤按鍵 | 小鍵盤 | 筆劃 | 說明 |
|----------|--------|------|------|
| **U** | Num 7 | 一（橫） | |
| **I** | Num 8 | 丨（豎） | |
| **O** | Num 9 | 丿（撇） | |
| **J** | Num 4 | 丶（點） | |
| **K** | Num 5 | フ（折） | |

- 輸入框可設定顯示「筆劃符號（一丨丿丶フ）」或「英文字母（u i o j k）」；由 `user\interfaceconfig.ini` 的 `showStrokeSymbols` 控制（1=筆劃符號，0=字母）。

### 3.2 字碼表格式

- 字碼表為 `system\Zi-Ma-Biao.txt`，UTF-8 編碼。
- 每行：**單字 + Tab + 筆劃碼**（筆劃碼僅含 u、i、o、j、k）。
- 範例：  
  `的	oikuuokj`  
  `一	u`  
  `是	ikuuuiuoj`

### 3.3 輸入流程

1. 確認為 **中文模式**（工具列模式顯示為中文）。
2. 依序按筆劃鍵（u/i/o/j/k），輸入框會顯示目前字碼（或筆劃符號）。
3. 程式依字碼查表，顯示 **候選字視窗**（一頁最多 9 個）。
4. 用 **數字鍵 1～9** 選字（第 1 個候選按 1，依此類推）。
5. 選定的字會送入「目前焦點」的應用程式（如記事本、瀏覽器輸入框）。

---

## 四、選字與翻頁

### 4.1 選字按鍵

| 操作 | 按鍵 |
|------|------|
| 選字 | **數字鍵 1～9**（對應候選第 1～9 個，僅數字選字） |
| 上一頁 | **↑** 或 候選視窗的「上一頁」按鈕 |
| 下一頁 | **↓** 或 候選視窗的「下一頁」按鈕 |

**說明**：本輸入法**僅支援以數字鍵 1～9 選字**，無空白鍵或 Enter 選字。一般模式下 Enter、Space 會傳給目前焦點程式。僅在**暫放模式**下，當焦點在暫放視窗、無候選且暫放區有內容時，Enter 才會被攔截並用來「送出暫放文字」。

### 4.2 輸入中功能鍵

| 按鍵 | 功能 |
|------|------|
| **Backspace** | 刪除最後一碼 |
| **ESC** | 取消輸入，清空字碼與候選，隱藏輸入/候選/聯想視窗 |

### 4.3 候選視窗右鍵選單（聯想條目管理）

在 **候選字** 或 **聯想字** 上按右鍵，可對「前字→此字」的聯想條目做設定：

- **⚐ 設為置頂 (pinned)** / **取消置頂 (unpin)**：該聯想條目置頂並加分，排序優先。
- **⚑ 設為鎖定 (locked)** / **取消鎖定**：該條目永遠排最前。
- **刪除此聯想**：從聯想學習中移除（之後仍可能再被學回）。
- **永不再顯示此聯想** / **取消永不再顯示**：該條目不再出現在候選中；可再選「取消」恢復。

---

## 五、中英文與標點

### 5.1 中英文切換

- **Shift 單擊**（按下後約 0.03～0.5 秒內放開，且未搭配其他鍵）：切換「中文+全形」與「英文+半形」。
- 工具列左側 **模式指示** 點擊也可切換。
- 托盤右鍵選單：「切換到英文模式」/「切換到中文模式」。

### 5.2 英文輸入

- **中文模式**下：僅 **u、i、o、j、k、P、L（或設定之萬用字元鍵）** 會當作筆劃/標點/萬用字；其餘 **A～Z** 會由輸入法轉成英文字母送入目標視窗（避免與系統 IME 衝突）。
- **Shift + 字母**：先清空當前輸入，再輸出該英文字母（大寫依 Caps Lock 與 Shift 決定）。

### 5.3 標點符號

- **P 鍵**（中文模式）：開啟 **全形標點符號選單**，內容來自 `user\punctmenu.txt`；以 1～9 選項，ESC 關閉選單。
- 鍵盤上 **, . / ; ' [ ] - = \ `** 及 **Shift+數字** 等：依標點表輸出全形/半形標點（中文模式多為全形，英文模式多為半形）。

---

## 六、聯想字（智能聯想）

### 6.1 何時出現聯想

- 選完一個字（或詞）後，程式會依「上一個選定的字/詞」推薦下一個字，顯示在 **聯想字視窗**。
- 聯想來源：字碼表內建的相鄰字對（bigram）＋ 個人學習檔 `user\context_learning.txt`。

### 6.2 聯想條目三種模式

| 模式 | 說明 |
|------|------|
| **locked（鎖定）** | 該「前字→後字」永遠排第一（⚑）。 |
| **pinned（置頂+加分）** | 置頂且加分，不易被洗掉（⚐）。 |
| **自動學習** | 依使用次數排序，有筆數上限。 |

- **永不再顯示**：在候選/聯想上右鍵選「永不再顯示此聯想」，該條目不再出現；可選「取消永不再顯示」恢復。

### 6.3 開關聯想字

- 工具列 **選單** → **聯想字功能**：可勾選/取消「✓ 聯想字功能」。
- 設定檔 `interfaceconfig.ini` → `[WindowBehavior]` → `enable_word_prediction=1` 或 `0`。

---

## 七、3+3 模式與萬用字元

### 7.1 萬用字元 *

- 輸入碼中可包含 **\***（星號）代表「任意一碼」，用於模糊查詢。
- 預設按鍵：**L**、**NumPad0**；可在 `user\interfaceconfig.ini` 的 `[InputSettings]` 中設定 `wildcardKey1`、`wildcardKey2`（支援 A–Z、NumPad0–9）。

### 7.2 3+3 建議

- 當輸入碼長度 ≥ 7 時，輸入框會提示「建議: 前三碼*後三碼」；例如前 3 碼 + * + 後 3 碼可縮小範圍、加快選字。
- 輸入框也會在適當長度時顯示「可用*號搜尋」「可用*號導出」等提示。

---

## 八、暫放視窗與剪貼簿模式

### 8.1 暫放模式

- **用途**：先輸入或貼上文字到暫放區，再一次性送出或另存，支援游標移動、Undo/Redo。
- **開啟方式**：工具列 **暫放** 按鈕，或托盤右鍵 →「開啟暫放模式」。
- **關閉**：再按一次工具列暫放按鈕；托盤在暫放模式下會顯示「發送暫放文字」「清空暫放區」。

### 8.2 暫放區操作

- **輸入**：焦點在暫放視窗時，可直接打字（含筆劃、英文、數字、標點）；中文模式仍可用 1～9 選候選。
- **游標**：方向鍵、Home/End；NumLock 關閉時小鍵盤 2/4/6/8 為下/左/右/上。
- **Backspace / Delete**：刪除游標前/後字元。
- **Enter**：若無候選則「發送暫放內容」到目前焦點視窗並清空暫放區。
- **送出 / 清除 / 儲存**：視窗上的按鈕可送出、清空或存成檔案（含時間戳檔名）。

### 8.3 剪貼簿模式

- 在 `interfaceconfig.ini` 的 `[WindowBehavior]` 中可設 `clipboard_mode=1` 開啟。
- 開啟後：可將暫放內容複製到剪貼簿，或從剪貼簿貼上後再送出；**Ctrl+V** 在暫放未取得焦點時會觸發「貼上到暫放」並清空暫放視窗等行為。

---

## 九、托盤與快捷功能

### 9.1 托盤圖示

- **左鍵單擊**：若已最小化則顯示主視窗；否則將主視窗置前。
- **右鍵**：開啟托盤選單。

### 9.2 托盤選單項目

| 項目 | 功能 |
|------|------|
| 顯示輸入法 / 顯示/置前 | 顯示主視窗或置前 |
| 切換到英文/中文模式 | 中英文切換 |
| 標點符號選單 | 開啟全形標點選單 |
| 發送暫放文字 / 清空暫放區 | 暫放模式下可用 |
| 開啟暫放模式 | 未開暫放時顯示 |
| 重置為滑鼠跟隨 | 取消固定位置 |
| 取消固定位置 | 在已固定時顯示 |
| 重新載入配置 | 重讀 interfaceconfig.ini 等 |
| 從GitHub更新字碼表 | 下載並覆蓋 Zi-Ma-Biao.txt |
| 關於 | 版本與簡要說明 |
| 重啟輸入法 | 關閉並重新啟動程式 |
| 關閉輸入法 | 結束程式 |

---

## 十、設定檔（interfaceconfig.ini）

設定檔位於 **user\interfaceconfig.ini**，修改後可透過「重新載入配置」或托盤「重新載入配置」套用（無需重啟程式）。

### 10.1 區段摘要

| 區段 | 主要內容 |
|------|----------|
| **[Colors]** | 主視窗、按鈕、候選字、聯想字、字碼輸入視窗、暫放視窗的背景/文字/選取/錯誤/邊框等顏色（支援 #RRGGBB） |
| **[Font]** | 主視窗、候選、字碼輸入、暫放視窗的字型名稱與大小 |
| **[Window]** | 視窗寬高（主視窗、候選、字碼輸入） |
| **[WindowBehavior]** | 置頂間隔、強制置頂、重新聚焦延遲、剪貼簿模式、半透明、聯想字開關、候選顯示字碼、筆劃符號顯示、螢幕模式變更提示 |
| **[InputSettings]** | 萬用字元鍵一、鍵二（如 L、NumPad0） |
| **[MultiScreenSettings]** | 多螢幕模式變更提示 |

### 10.2 常用選項範例

```ini
[WindowBehavior]
enable_word_prediction=1    ; 1=聯想字開啟
showStrokeSymbols=1         ; 1=輸入框顯示一丨丿丶フ，0=顯示 uiojk
showCandidateCode=0         ; 1=候選顯示英文字碼
force_stay_on_top=1         ; 1=強制視窗置頂
enable_transparency=0       ; 1=半透明
transparency_alpha=220      ; 0–255
clipboard_mode=0            ; 1=剪貼簿模式

[InputSettings]
wildcardKey1=L
wildcardKey2=NumPad0
```

---

## 十一、進階與疑難排解

### 11.1 暫停輸入法

- 選單 → **❚❚ 暫停輸入法**：會釋放鍵盤鉤子，按鍵不再被攔截，適合暫時使用其他輸入方式或遊戲。
- 再選 **▶ 啟用輸入法** 即可恢復。

### 11.2 按鍵放行（不攔截）

- **Ctrl + 任意鍵**、**Win 鍵**、**Alt 鍵** 一律放行，不攔截，故 Win+R、Win+D、Alt+Tab、Ctrl+C/V 等系統快捷鍵可正常使用。
- 剪貼簿模式下的 **Ctrl+V** 會由輸入法處理「貼上到暫放」後再放行給系統。

### 11.3 與系統 IME 的關係

- 衝突避免是透過**鍵盤鉤子攔截**：需要由本輸入法處理的按鍵（筆劃鍵、數字選字、標點、功能鍵等）在鉤子裡被攔截並回傳消耗（不傳給下一個處理程式），因此不會送到 Windows 內建 IME，也就不會發生雙重處理。
- 若仍遇衝突，可先將系統預設輸入法切到「英文」鍵盤配置再使用本程式。

### 11.4 字碼表更新

- 選單或托盤 → **從GitHub更新字碼表**：會下載並覆蓋 `system\Zi-Ma-Biao.txt`，若有自訂請先備份。
- 若啟動時偵測不到字碼表，程式會嘗試自動下載；失敗時會提示手動下載並放入 `system` 目錄。

### 11.5 位置與多螢幕

- 視窗位置會寫入 `system\positions.ini`，支援延伸/鏡像等多螢幕模式。
- 「重置為滑鼠跟隨」會改回依滑鼠位置顯示，並可再次拖曳固定位置。

### 11.6 使用者字典與聯想學習

- **user_dict.txt**：使用者選過的字/詞會學習並影響排序與聯想。
- **context_learning.txt**：記錄聯想條目（前字、後字、次數、locked/pinned 等），為 UTF-8（含 BOM）；手動編輯時請保留格式與註解。

---

## 十二、快速參考

| 項目 | 說明 |
|------|------|
| 筆劃鍵 | U=橫、I=豎、O=撇、J=點、K=折（小鍵盤 7/8/9/4/5 同） |
| 萬用字元 | L 或 NumPad0（可設）→ * |
| 標點選單 | P（中文模式） |
| 選字 | 僅數字鍵 1～9（對應候選 1～9） |
| 翻頁 | ↑ / ↓ 或候選窗按鈕 |
| 取消輸入 | ESC |
| 中英切換 | Shift 單擊 或 工具列模式鈕 |
| 聯想右鍵 | 置頂(pinned)、鎖定(locked)、刪除、永不再顯示 |

---

以上內容依目前程式碼與專案分析整理，若與實際介面或版本有出入，以執行檔行為為準。更多技術細節可參考專案內 **ime\專案分析.md**。
